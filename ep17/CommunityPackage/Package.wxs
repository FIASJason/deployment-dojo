<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs">
    <Package Id="TODO_Manufacturer.CommunityPackage" Name="~CommunityPackage" Manufacturer="Dojo" Version="1.0.0.0">
        <MajorUpgrade DowngradeErrorMessage="!(loc.DowngradeError)" />

        <!-- Set a property, used in the WixLib -->
        <!-- Comment this line to see what happens when the variable is not defined and binding variables !() are used -->
        <WixVariable Id="DojoType" Value="Community" />
        <Feature Id="Main">
            <ComponentGroupRef Id="ExampleComponents" />
        </Feature>
    </Package>
</Wix>